session: config

root: ~/.config

windows:
  - name: config
    layout: main-horizontal
    commands:
      - $EDITOR .
  - name: nvim
    layout: main-horizontal
    commands:
      - $EDITOR ./nvim
  - name: kitty
    layout: main-horizontal
    commands:
      - $EDITOR ./kitty/kitty.conf
  - name: tmux
    layout: main-horizontal
    commands:
      - $EDITOR ./tmux/tmux.conf
