session: nbr-haus

root: ~/Documents/dev/repos/nbr.haus/

stop:
  - killall node

windows:
  - name: code
    commands:
      - nvim .
  - name: run
    commands:
      - clear
  - name: server
    commands:
      - npm run dev
