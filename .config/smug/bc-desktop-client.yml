session: bc-desktop-client

root: ~/Documents/dev/repos/dealercenter-digital@bc-desktop-client/

stop:
  - docker kill $(docker ps -q); true
  - killall node; true

windows:
  - name: code
    layout: main-horizontal
    commands:
      - nvim .
  - name: run
    commands:
      - tmux-2x2-grid
