session: bc-desktop-client

root: ~/Documents/dev/repos/dcd/bc-desktop-client

stop:
  - docker kill $(docker ps -q); true
  - killall node; true

windows:
  - name: code
    layout: main-horizontal
    commands:
      - nvim .
  - name: server
    commands:
      - tmux-server-setup
