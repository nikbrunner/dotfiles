session: bc-tools

root: ~/Documents/dev/repos/dcd/bc-tools

windows:
  - name: code
    root: .
    layout: main-horizontal
    commands:
      - nvim .
  - name: run
    commands:
      - tmux-2x2-grid
